# SPARTA-MSMC Example

This repository provides one documented, self-contained simulation example:

- `msmc_circle/`: hypersonic argon flow over a half-cylinder using MSMC on a
  50 x 25 base grid.

See `msmc_circle/README.md` for the complete build, run, output, and
post-processing workflow.
